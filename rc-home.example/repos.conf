# Format: <name> <git-url>
# The name is also the file name of the deploy key: keys/<name>.key
# The FIRST repo in the list is the default repo ("rc new" without a repo).
voenix-shop-kotlin git@github.com:bitstorm-tech/voenix-shop-kotlin.git
